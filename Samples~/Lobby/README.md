Drop in `Prefabs/UI/SimpleLobbyUI.prefab` and set `gameSceneNames` on the scene instance.

These **game** assets still live in the project (same GUIDs as the starter template):

- `NetworkManager` prefab (`CustomNetworkManager`)
- Default network prefabs list
- `PlayerNetworkIdentity` prefab (NGO player prefab)
- `PauseMenu` prefab

Copy those from the starter project, then import this sample.
